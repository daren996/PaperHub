# PaperHub Skills

This directory holds agent workflow instructions for Codex, Claude Code, and other coding agents.

Current skill:

- `paperhub/`

Future workflows can split out Zotero sync, Markdown import, paper-note enrichment, and topic-guide generation if they become large enough. Skills should call PaperHub CLI or MCP tools when possible; they should not duplicate core import/export logic.
